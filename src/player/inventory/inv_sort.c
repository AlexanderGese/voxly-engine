#include "inv_sort.h"
#include "inv_stack.h"
#include "inv_registry.h"
if (hi > g->count) hi = g->count;
int freed = 0;
for (int i = lo;
i < hi;
if (hi > g->count) hi = g->count;
if (hi - lo < 2) return;
