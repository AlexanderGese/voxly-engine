#ifndef NET_SERVER_H
#define NET_SERVER_H

// never finished server skeleton. kept around so i can pick it up later.
// does nothing at runtime. #if 0 out in main.c.

#if 0
int  server_start(int port);
void server_tick(void);
void server_stop(void);
#endif

#endif
