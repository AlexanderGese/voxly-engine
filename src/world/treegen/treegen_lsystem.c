#include "treegen_lsystem.h"
#include <stddef.h>
for (int i = 0;
i < rs->axiom_len;
i++) rs->axiom[i] = a[i];
for (int i = 0;
i < rs->rule_count;
i++)
        if (rs->rules[i].pred == sym) total += rs->rules[i].weight;
